name: Sync to Hugging Face Spaces

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push directly to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # 1. Configuration de l'identité de l'Action
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          
          # 2. Envoi forcé vers le dépôt distant Hugging Face
          # ⚠️ REMPLACE "Examen-Git-IABD" ci-dessous par le nom EXACT de ton Space sur ton écran !
          git push --force https://ornellaFOUDA:$HF_TOKEN@huggingface.co/spaces/ornellaFOUDA/Examen-Git-IABD main:main